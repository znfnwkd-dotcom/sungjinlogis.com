---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const title = 'Matte + Long-life Continuous Filtration | SUNGJINLOGIS';
const description =
  'Long-life continuous filtration controls particles and suspended solids to maintain stable matte appearance uniformity and color consistency.';

const highlights = [
  {
    title: 'Long-life Continuous Filtration',
    desc: 'Continuously reduces fine particles and suspended solids in real time to lower the risk of surface defects (specks/haze/color stains).',
  },
  {
    title: 'Appearance & Color Consistency',
    desc: 'Maintains bath cleanliness and stability to minimize variation in matte texture and color.',
  },
  {
    title: 'Production Stability',
    desc: 'Reduces process drift in continuous production to manage defect rates and rework risk.',
  },
];

const controlPoints = [
  'Reduce fine particles and suspended solids generated during operation through continuous recirculating filtration',
  'Maintain cleanliness by operating based on filtration flow rate and turnover (circulation volume) standards',
  'Respond immediately to differential pressure (ΔP) rise with filter replacement/cleaning (clogging/saturation control)',
  'Prevent re-suspension by regularly removing sludge and settled solids from the tank bottom',
  'Minimize analytical variability by standardizing sampling location and timing (data reliability)',
  'When needed, strengthen carbon treatment/filtration to reduce organic contamination and fine particles simultaneously',
];

const evidence = [
  'In-house thickness/appearance/adhesion evaluation and data-based management before/after condition changes',
  'Support reliability testing such as salt spray (including CASS when required), thermal shock, and heat & humidity cycles',
  'Secure process traceability through records such as filter replacement history, differential pressure (ΔP), and flow rate',
  'Process control and 대응 based on quality system requirements (SQ/ISO, etc.)',
];

const applications = [
  'Matte appearance–sensitive parts such as automotive interior trims and buttons',
  'Products with high appearance requirements such as mobile brackets and decorative parts',
  'Parts where stabilizing downstream nickel/chrome appearance quality is critical',
];

const gallery = [
  {
    src: '/images/capabilities/matte-satin/longlife-filter-01.png',
    alt: 'Long-life continuous filtration system',
  },
];
---

<BaseLayout title={title} description={description} lang="en" pathPrefix="/en">
  <!-- HERO -->
  <section class="rounded-3xl border border-stroke bg-base p-6 shadow-soft">
    <a class="text-sm text-accent hover:underline" href="/en/capabilities/">
      ← Capabilities
    </a>

    <h1 class="mt-2 text-3xl font-semibold tracking-tight md:text-4xl">
     Matte + Long-life Continuous Filtration
    </h1>

    <p class="mt-3 text-sm md:text-base leading-relaxed !text-sub drop-shadow-md">
      SUNGJINLOGIS manages <span class="text-white font-semibold">particles and suspended solids</span> in the bath through
      <span class="text-white font-semibold"> long-life continuous filtration</span>, maintaining
      <span class="text-white font-semibold"> uniform matte appearance</span> and stable
      <span class="text-white font-semibold"> color consistency</span>.
    </p>

    <div class="mt-6 grid gap-4 md:grid-cols-3">
      {highlights.map((h) => (
        <div class="rounded-2xl border border-stroke bg-surface p-5 shadow-soft">
          <div class="text-sm font-semibold text-white">{h.title}</div>
          <p class="mt-2 text-sm text-sub leading-relaxed">{h.desc}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- Control Points -->
  <section class="mt-10 rounded-3xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">Control Points</h2>
    <p class="mt-2 text-sm text-sub">
      A summary of key control items for consistent matte/semi-matte appearance.
    </p>

    <div class="mt-4 grid gap-3 md:grid-cols-2">
      {controlPoints.map((t) => (
        <div class="rounded-xl border border-stroke bg-surface p-4 text-sm text-sub">
          • {t}
        </div>
      ))}
    </div>
  </section>

  <!-- Verification Evidence -->
  <section class="mt-10 rounded-3xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">Verification Evidence</h2>
    <p class="mt-2 text-sm text-sub">
      We ensure process reliability through measurable data and systems.
    </p>

    <div class="mt-4 grid gap-3">
      {evidence.map((t) => (
        <div class="rounded-xl border border-stroke bg-surface p-4 text-sm text-sub">
          • {t}
        </div>
      ))}
    </div>
  </section>

  <!-- Typical Applications -->
  <section class="mt-10 rounded-3xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">Typical Applications</h2>
    <p class="mt-2 text-sm text-sub">Applied to product groups with high appearance-quality requirements.</p>

    <div class="mt-4 grid gap-3 md:grid-cols-2">
      {applications.map((t) => (
        <div class="rounded-xl border border-stroke bg-surface p-4 text-sm text-sub">
          • {t}
        </div>
      ))}
    </div>
  </section>

  <!-- Equipment Photos -->
  <section class="mt-10 rounded-3xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">Equipment Photos</h2>
    <p class="mt-2 text-sm text-sub">An example of a long-life continuous filtration setup.</p>

    <div class="mt-4 grid gap-4 md:grid-cols-2">
      {gallery.map((g) => (
        <img
          src={g.src}
          alt={g.alt}
          class="w-full rounded-2xl border border-stroke bg-surface object-cover"
          loading="lazy"
        />
      ))}
    </div>
  </section>
</BaseLayout>
