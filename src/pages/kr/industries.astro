---
import BaseLayout from '../../layouts/BaseLayout.astro';

const title = 'Industries | 성진로지스';
const description = '자동차 내장재 및 모바일 카메라 브라켓 등 적용 분야';
---
<BaseLayout title={title} description={description} lang="ko" pathPrefix="/kr">
  <section class="rounded-xl2 border border-stroke bg-surface p-8 shadow-soft">
    <h1 class="text-2xl font-semibold">Industries</h1>
    <p class="mt-2 text-sm text-sub">고급 외관과 안정성이 필요한 산업군에 제공합니다.</p>

    <div class="mt-8 grid gap-4 md:grid-cols-2">
      <div class="rounded-xl border border-stroke bg-base p-6">
        <div class="text-sm font-semibold">Automotive Interior</div>
        <p class="mt-1 text-sm text-sub">내구·외관 기준을 만족하는 내장 부품</p>---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const title = '적용산업 | 성진로지스';
const description =
  '자동차 내장, 모바일 카메라 브라켓 등 양산 검증된 플라스틱 도금 적용 산업과 확장 가능한 산업 영역을 소개합니다.';

const productionIndustries = [
  {
    title: 'Automotive Interior',
    subtitle: '자동차 내장 장식/조작부품 (양산)',
    badge: '양산',
    bullets: [
      '스위치/노브/베젤/버튼류 등 외관 민감 부품',
      '외관(광택/컬러) 균일 + 내식/내구 균형 설계',
      '요구 사양(두께/후단/외관) 기준 공정 운영',
    ],
    note: '차종/고객사/프로그램 상세는 NDA 체결 후 제공',
  },
  {
    title: 'Mobile Device — Camera Bracket',
    subtitle: '모바일 카메라 브라켓/데코 부품 (양산)',
    badge: '양산',
    bullets: [
      '정밀 외관 품질(점/흐림/색편차) 관리 중심',
      '무광/여과/오염 관리로 결점 리스크 저감',
      '검증 데이터 기반 공정 재현성 관리',
    ],
    note: '고객 요구사항(외관/색상/내식) 기준으로 조건 최적화',
  },
];

const expandableIndustries = [
  {
    title: 'IT / Consumer Electronics Decoration',
    subtitle: 'IT/전자기기 외관 데코 및 프리미엄 외관 부품',
    badge: '적용 가능',
    bullets: [
      '베젤/프레임/데코 파트 등 고급 외관 중심 부품군',
      '광택·컬러 균일/미세 결점 관리로 프리미엄 외관 품질 대응',
      '양산 재현성 기반 공정 조건 표준화 및 이력 관리',
    ],
  },
  {
    title: 'Industrial HMI / Control Panel / Housing',
    subtitle: '산업용 HMI·조작패널·하우징 외관 부품',
    badge: '적용 가능',
    bullets: [
      '조작부/패널/하우징 등 사용 환경 기반 내구·외관 요구 대응',
      '스크래치/오염/세정 내성 등 요구 특성에 맞춘 구성 설계',
      '시험·검증 체계 기반 품질 대응(요구 시험 조건 협의 가능)',
    ],
  },
  {
    title: 'Home Appliance & Smart Home',
    subtitle: '가전/스마트홈 조작부품(프리미엄 외관)',
    badge: '적용 가능',
    bullets: [
      '다이얼/노브/버튼/베젤 등 사용자 접점 외관 파트',
      '생활오염·세정제 내성/스크래치 대응',
      '외관 균일 및 대량생산 안정화 중심 운영',
    ],
  },
];


const partFamilies = [
  'Switch / Keyboard Bar / Button',
  'Knob / Dial / Bezel',
  'Camera Bracket / Decorative Ring',
  'Trim / Frame / Cover',
];

const keyRequirements = [
  { k: '외관', v: '광택·컬러 균일, 결점(점/흐림) 저감' },
  { k: '내식/내구', v: '니켈/크롬 구성 최적화 + 후처리 연계' },
  { k: '재현성', v: '표준 조건/데이터 기반 운영(공정 흔들림 최소화)' },
  { k: '규제 대응', v: 'Hex-free(친환경 3가 크롬) 등 요구 대응' },
];
---

<BaseLayout title={title} description={description} lang="ko" pathPrefix="/kr">
  <section class="rounded-2xl border border-stroke bg-base p-6 shadow-soft">
    <h1 class="text-3xl font-semibold tracking-tight md:text-4xl">Industries</h1>
    <p class="mt-3 text-sm md:text-base leading-relaxed text-sub">
      성진로지스는 양산 검증된 산업을 중심으로, 요구 사양(외관/내식/두께/후단)에 맞춘 플라스틱 도금 공정을 제공합니다.
    </p>
  </section>

  {/* 양산 적용 */}
  <section class="mt-10 rounded-2xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">양산 적용</h2>
    <p class="mt-2 text-sm text-sub">현재 생산(또는 양산 운영 경험) 기반의 적용 산업입니다.</p>

    <div class="mt-4 grid gap-4 md:grid-cols-2">
      {productionIndustries.map((x) => (
        <div class="rounded-2xl border border-stroke bg-surface p-5 shadow-soft">
          <div class="flex items-start justify-between gap-3">
            <div>
              <div class="text-sm font-semibold text-white">{x.title}</div>
              <div class="mt-1 text-sm text-sub">{x.subtitle}</div>
            </div>
            <span class="shrink-0 rounded-full border border-stroke bg-base px-3 py-1 text-xs text-white/80">
              {x.badge}
            </span>
          </div>

          <ul class="mt-4 space-y-2 text-sm text-sub">
            {x.bullets.map((b) => (
              <li>• {b}</li>
            ))}
          </ul>

          {x.note && (
            <div class="mt-4 rounded-2xl border border-stroke bg-base p-3 text-xs text-white/70">
              {x.note}
            </div>
          )}
        </div>
      ))}
    </div>
  </section>

  {/* 적용 가능 */}
  <section class="mt-10 rounded-2xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">적용 가능 산업</h2>
    <p class="mt-2 text-sm text-sub">프로젝트 요구 사양에 따라 확장 가능한 적용 분야입니다.</p>

    <div class="mt-4 grid gap-4 md:grid-cols-3">
      {expandableIndustries.map((x) => (
        <div class="rounded-2xl border border-stroke bg-surface p-5 shadow-soft">
          <div class="flex items-start justify-between gap-3">
            <div>
              <div class="text-sm font-semibold text-white">{x.title}</div>
              <div class="mt-1 text-sm text-sub">{x.subtitle}</div>
            </div>
            <span class="shrink-0 rounded-full border border-stroke bg-base px-3 py-1 text-xs text-white/70">
              {x.badge}
            </span>
          </div>

          <ul class="mt-4 space-y-2 text-sm text-sub">
            {x.bullets.map((b) => (
              <li>• {b}</li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </section>

  {/* 대표 부품군 / 요구특성 */}
  <section class="mt-10 rounded-2xl border border-stroke bg-base p-6 shadow-soft">
    <h2 class="text-lg font-semibold">대표 부품군 & 요구특성</h2>
    <p class="mt-2 text-sm text-sub">적용 산업 전반에서 공통으로 요구되는 항목을 요약했습니다.</p>

    <div class="mt-4 grid gap-4 md:grid-cols-2">
      <div class="rounded-2xl border border-stroke bg-surface p-5">
        <div class="text-sm font-semibold text-white">대표 부품군</div>
        <ul class="mt-3 space-y-2 text-sm text-sub">
          {partFamilies.map((p) => (
            <li>• {p}</li>
          ))}
        </ul>
      </div>

      <div class="rounded-2xl border border-stroke bg-surface p-5">
        <div class="text-sm font-semibold text-white">핵심 요구특성</div>
        <div class="mt-3 space-y-2 text-sm text-sub">
          {keyRequirements.map((r) => (
            <div class="rounded-2xl border border-stroke bg-base p-3">
              <span class="text-white/90 font-semibold">{r.k}</span>
              <span class="text-white/70"> — {r.v}</span>
            </div>
          ))}
        </div>
      </div>
    </div>

    <div class="mt-6 rounded-2xl border border-stroke bg-surface p-5 text-sm text-sub">
      문의 시 아래 정보를 주시면 더 정확한 공정 제안을 드릴 수 있습니다: <br />
      • 소재(ABS/PC/PC+ABS) • 외관(광택/무광/컬러) • 내식 요구(시험 조건) • 목표 두께 • 후단 공정(크롬/후처리 등)
    </div>
  </section>
</BaseLayout>

      </div>
      <div class="rounded-xl border border-stroke bg-base p-6">
        <div class="text-sm font-semibold">Mobile Device — Camera Bracket</div>
        <p class="mt-1 text-sm text-sub">정밀 외관 부품의 컬러/결함 관리</p>
      </div>
    </div>
  </section>
</BaseLayout>
